/**
 * 로그인이 필요한 라우트들
 * @param string 전체 라우트 또는 공통 라우트
 */
export const authRequeiredRoutes = ["/myssg/"]

/**
 * 이 값으로 시작하는 라우트는 authentication API에 사용됨
 * @type {string}
 */
export const apiAuthPrefix = "/api/auth"
